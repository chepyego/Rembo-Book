<div class="hidden">
    <nav class="flex">
        <ul class=" flex flex-row items-center gap-10 mx-auto bg-white py-1 shadow-sm w-full px-2 rounded">
            <li class="font-medium">Dashboard</li>
            <li>
                <% if current_user&.staff? %>
                  <button class=" font-medium flex m-6 ml-auto"><%= link_to "New service", new_service_path, class: " flex  justify-end mr-auto rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %></button>
               <% end %>
            </li>
            <li class="font-medium">Bookings</li>
            <li class="font-medium"><%= button_to "Logout", session_path, method: :delete, class: " text-gray-900 hover:text-pink-600" if authenticated? %></li>


        </ul>
    </nav>
</div>

<div>
  <nav class="w-full bg-white shadow-sm rounded px-4 sm:px-6 py-4 sm:py-3">
    
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 sm:gap-0">
      
      <ul class="flex items-center justify-between sm:justify-start gap-6 sm:gap-8 border-b sm:border-none pb-3 sm:pb-0">
        <li class="font-semibold text-gray-800 text-lg">Dashboard</li>
        <li class="font-medium text-gray-600 hover:text-gray-900 transition-colors text-sm sm:text-base">Bookings</li>
      </ul>

      <div class="flex items-center justify-between sm:justify-end gap-4 sm:gap-6 w-full sm:w-auto">
        
        <div class="font-medium text-sm sm:text-base">
          <%= button_to "Logout", session_path, method: :delete, class: "text-gray-600 hover:text-pink-600 transition-colors" if authenticated? %>
        </div>

        <% if current_user&.staff? %>
          <%= link_to "New service", new_service_path, class: "inline-flex items-center justify-center text-center rounded-lg py-2.5 sm:py-2 px-4 bg-blue-600 hover:bg-blue-700 text-white font-medium text-sm transition-colors shadow-sm flex-1 sm:flex-none" %>
        <% end %>
      </div>

    </div>
  </nav>
</div>